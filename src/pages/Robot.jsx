import { useLoaderData } from "react-router-dom";

export async function loader({ params }) {
    // API Call for the backend to get the details of the robot
    return null;
}

export default function Robot() {}
